<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Include shared preferences for backup -->
        <include domain="sharedpref" path="user_preferences.xml"/>
        <include domain="sharedpref" path="game_progress.xml"/>
        
        <!-- Include database for backup -->
        <include domain="database" path="mete_database.db"/>
        
        <!-- Exclude cache and temporary files -->
        <exclude domain="file" path="cache"/>
        <exclude domain="file" path="tmp"/>
        <exclude domain="file" path="no_backup"/>
        
        <!-- Exclude large media files -->
        <exclude domain="file" path="media/" />
        <exclude domain="file" path="videos/" />
    </cloud-backup>
    
    <device-transfer>
        <!-- Include everything for device transfer except cache -->
        <include domain="sharedpref" path="."/>
        <include domain="database" path="."/>
        <include domain="file" path="."/>
        <exclude domain="file" path="cache"/>
        <exclude domain="file" path="no_backup"/>
    </device-transfer>
</data-extraction-rules>